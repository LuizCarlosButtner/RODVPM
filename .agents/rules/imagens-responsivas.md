---
trigger: always_on
---

Seu arquivo MD está pronto
[file-tag: code-generated-file-0-1783191757371988415]

O arquivo gerado contém um sistema de diretrizes detalhado pronto para ser utilizado como *prompt* (instruções de sistema) para qualquer IA. Ele abrange:

* **Regras claras de uso:** Explicando as diferenças entre os descritores `w` (largura) e `x` (densidade de pixels).
* **Obrigatoriedade do atributo `sizes`:** Como a IA deve calcular as *media queries* com base no comportamento responsivo (Mobile-First) para informar o navegador antes do carregamento do CSS.
* **Otimização de LCP e CLS:** Inclusão de atributos como `width`/`height` (evitando saltos de layout) e a utilização correta de `loading="lazy"` e `fetchpriority="high"`.
* **Elementos `<picture>`:** Orientação para usar este elemento quando for necessário aplicar formatos modernos (como WebP/AVIF) ou direção de arte (cortes diferentes para mobile e desktop).
* **Exemplos Práticos:** Modelos práticos de "Antes e Depois" para que a IA tenha referências claras do formato esperado na saída.